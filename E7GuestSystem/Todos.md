- [ ] We need to make some adjustments to the language in our documents related to the E7GuestSystem. The account users & admin are the only ones that will use or regularly access the app for updates, visitor followup messages, & other management features. Visitors will only fill out a form when they visit. They have no need of the dedicated app. Their access will be through the browser / web form opened via link or QR code scan.  The "app" would be used to access the CRM / data aggregation features. a visitor could install the app, but that would only serve as a preview platform to entice them to create an account if they need our SaaS product. The adjustments are needed in the E7GuestSystem documents and the app-distribution-fee-avoidance.md document.
- [ ] We noticed in the schema that the visitor's referredBy note mentions an ID. This will usually be empty and the user will only know a name. This should be a string.
- Let's change visitors isLocal:bool & isMoving:bool into visitType:enum (local, moving, traveling, searching)
- the user / message tables are not properly designed for messaging platform. The messaging system will work similar to Slack in that each visitor that opted in to followup can be messaged by any of the the account users.  The user will be able to view the entire conversation history as a chat + user added notes about the visitor so that any other users can see previous messages & responses and will be able to followup on any conversation.  The visitor will only see the text messages, but the app users will see what other users have sent and what replies have been returned + user added notes (internal user access only)
- Reminders will be user specific & local only. E.g. User 1 sets the a daily reminder at 2pm to check and follow up with visitors 
