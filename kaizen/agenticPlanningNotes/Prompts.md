- v1 notes
	1. This looks great, but we need some edits:
	2. The nodes  & edges don't move together.  Dragging the canvas moves all the labels / nodes, but not the edges. Dragging one node causes the edges to move and update, but the connections don't really look connected to the nodes & sometimes disappear.
	3. The nodes & zones are too close together, there needs to be space between the nodes and sections to make it clearer
	4. We want zoom in & zoom out controls, we'd like buttons and also use the mouse wheel to control the zoom
- Note also, I moved the files into the /home/todd/e7/kaizen/agenticPlanningNotes folder to keep the data organized.
- v2  notes, file:///home/todd/e7/kaizen/agenticPlanningNotes/agentic-harness-interactive.html
	- Make the overview map interactive so the user can move the active view around using the overview map active rectangle
	- Make the mousewheel zooming less sensitive, it's zooms too fast to control 
	- When trying to move nodes, they are jumping out of the zone to other areas of the screen.- This looks great, but let's make the zone sections 3x larger and add 2x the space between nodes, they're still too close.
- v3
	- Let's add a spread/contract feature that can be used to adjust how tight all the nodes are
	- Let's add a select to zoom so I can drag a rectangle around the area I want to zoom in to see
	- Reverse the mousewheel zoom directions
	- add a confirmation step when deleting nodes
- v3
	- 
- v3
	- 
- v3
	- 
- v3
	- 